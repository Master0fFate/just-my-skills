# Worker audit transfer and current review

## Coverage and limits

This is a historical review snapshot, not the final disposition. See
[the final audit record](skill-audit.md) for fixes, executed checks, and remaining
limits. The parent subsequently corrected the metadata, non-readiness routing,
HTML safeguards, review-mode checks, image routing, and packaging findings below.

All three audit messages were received: `/root/sexyness-auditor`, `/root/specialist-auditor`, and `/root/workflow-auditor`. None is missing. Their findings below are attributed summaries, not new runtime evidence. Each auditor reported read-only work without edits or executed evaluations. Their snapshots can precede later parent fixes.

For this report, `/root/workflow-builder` independently read **current** Shipshape and all six references, both templates, and agent metadata; Explain; and Retrofit. The three new Shipshape references are `process-simplification.md`, `refinement.md`, and `scored-audit.md`. Only this report was written in this follow-up. No network, installation, commit, browser run, or skill edit occurred.

## Current independent findings — workflow-builder

1. **Shipshape metadata still conflicts with neutral mode selection.** `skills/shipshape/agents/openai.yaml` enables implicit invocation while its default prompt says “fix its highest-priority gaps.” The core correctly defaults ambiguous requests to Audit and requires scoped authority. Make metadata mode-neutral so assessment requests do not acquire an improvement instruction. This is a conflicting signal, not observed unauthorized execution.
2. **Shipshape still imposes a milestone frame on non-readiness work.** Its description, intake, mandatory verdict section, readiness reference, and report template require a named milestone. New policy/scored-audit/refinement capabilities need an objective and criteria, not necessarily a shipping stage. Make readiness conditional; allow a direct assessment conclusion. Otherwise the merger retains avoidable ceremony and a misleading output frame.
3. **Explain: remaining HTML safeguards.** `skills/explain/SKILL.md` fixes the old priority/persona lock, compulsory insults, unsupported learning claims, and endless tone loop. It preserves concrete teaching, phone-first HTML, text overrides, actual media capability, keyboard/focus/contrast checks, themes, and honest unverified rendering. It still lacks explicit escaping of untrusted topic content and text equivalents for meaningful diagrams. Add these; name reduced-motion preference handling rather than only “motion optional.” These are instruction gaps, not demonstrated vulnerabilities in a generated page.
4. **Retrofit: qualify executable checks.** `skills/retrofit/SKILL.md` now protects plan-only work, scopes production/destructive actions, states unavailable-source limits, and requires safe copies plus verified recovery for destructive conversion. Unique adapter/incremental/mechanical/parallel strategies and old-to-new edge tests remain. Its verification list still includes “migrations, formatters” without check-only/disposable qualification. Explicitly prohibit their mutating forms in review mode; migration tests need isolated data and authorization.
5. **Shipshape merger content is otherwise sound.** Process simplification preserves challenge → safe deletion → simplification → measured acceleration → stable automation, dependencies, human control, logs, rollback, and no deletion quota. Refinement preserves severity, adjacent regression checks, evidence limits, and partial-versus-pass distinction. Scored audit preserves dimensions, material findings, declared independence limits, unknown/unassessed values, prior anchors/weights, and hard gates over averages. No fake credentials, fixed finding quotas, or tool claims were found. Audit remains read-only; examples explicitly are not test results. Concerns 1–2 remain the main integration issues.

## Received sexyness audit — sexyness-auditor

The auditor fully read original/current root and subskills, all owned references, design CSS, Humanizer notice, selected toolkit manuals/scripts, and installed Pi skill-discovery documentation. No full vendor-script or runtime audit was claimed.

**Current-snapshot issues:**

- `.gitignore:4` retained an obsolete toolkit entry; `skills/README.md:71` still promised a frontend browser toolkit after its removal. Remove stale promises and sweep dotfiles/ignored content. The remaining `animate-sexyness` migration-map text was not a broken link.
- Imagegen lines 26–27 skip generation when a suitable asset/exact target exists, conflicting with direct generation/edit requests in lines 18–19. Apply this skip only to optional implementation exploration.
- Performance lines 42–54 still own broad structural cleanup despite routing local cleanup to code and structural boundaries to architecture. Keep measured cost-driven cleanup; hand off shape-only campaigns.
- Nested skills now have `disable-model-invocation: true`, but root discovery omitted explicit accessibility/a11y/WCAG, image-generation, and non-code layering triggers. Add concise routes. The flag hides automatic prompt entries; it does **not** remove recursive discovery or explicit commands.
- Typography in `design-sexyness/references/layout-and-resilience.md:17–23` was rewritten from pre-existing uncommitted content. New claims are more accurate, but byte-preservation was not achieved. Confirm rewrite authorization; if restoration is needed, restore the prior working block, not HEAD.
- Toolkit removal lost native QA specifics: simulator/emulator/device evidence, light/dark and enlarged text, and simulator-versus-hardware performance limits. Preserve these briefly. Restore observed device settings after tests, not hardcoded defaults.

**Preserved strengths and merger limits:** Motion→smoothness retains interruption, FLIP/shared elements, bounded staggers, fallbacks/cleanup, stale-result rejection, rollback, duplicate mutation checks, drafts, and failure/race tests. Screenshots do not prove interruption. Read-only guards and local links were sound. Root/code/architecture/performance ownership is workable if consistent. Keep architecture's non-code contracts, performance evidence, async recovery, design resilience/Fibonacci, copy fidelity/license, and platform-aware accessibility. Basic focus/semantics/reduced motion cannot be optional. Exact Accelerate parity was not audited. Deleted live overlays, detectors, and composition measurement executables are **retired**, not preserved by prose. Toolkit removal resolves inherited forced tool/agent/network/update/instruction-authority conflicts.

## Received specialist audit — specialist-auditor

**Angelcore: retain its distinct reference-locked visual system and deterministic image/terminal tools.** Route it only for explicit Angelcore/Fate/reference-style requests, not generic polish. Material issues:

- Stored evidence reports v5.0 and old boxed inputs while current CSS/changelog describe v5.1. Historical 46 Python/107 browser/47 package passes do not verify the current revision; label or regenerate.
- `tools/audit.py:29–36` omits `references/no-cages.md`, dev requirements, three unit-test files, and fixtures; lacks a no-cage check; missing tokens/CSS can crash after recording failure. Add missing-file regressions and computed idle-border checks. Color scans alone cannot prove affordance.
- README permits removing an unlicensed reference screenshot, but audit/build expect it. Rights are unknown. Define public/private or user-provided-reference modes.
- `tests/browser_check.py:69` always uses Chromium `--no-sandbox`; offline mode still starts a server; README's HTTP-server command binds beyond loopback. Default to sandbox/loopback, make isolated-only overrides explicit, and skip the server offline.
- `examples/web/demo.js:47–74` recreates selected button lists and likely loses focus; add keyboard continuity tests. Browser checks cover only one radius corner, selected hit targets, limited forced-colors behavior, and lack later-page error/HTTP-asset checks.
- ASCII conversion records lack crop, source fingerprint, and custom glyph-coverage inputs. Preserve reproducibility plus existing size/alpha/EXIF/binary/overwrite safeguards.

**Current Calibrate review: accepted, no material safeguard regression.** Full current core/reference read retained explicit invocation, one question, conditional evidence/falsification, no diagnosis/sensitive inference, exact approval, destination approval, conversation-only notes, and unrelated-content preservation. All 12 areas remain optional; skip/stop/partial output replace forced completion. Nonblocking refinements: explicitly obtain approval for changed draft text before saving; inspect the intended destination for an existing profile even if none was mentioned.

**Other conclusions:** Retain Explain's visual-teaching purpose; its original priority/persona/insult contradictions motivated the rewrite assessed above. Merge Universal Auditor into Shipshape as optional vectors/scoring, not certification. Preserve read-only default, authorized observable-work assessment, unknowns, zero findings when warranted, requested anchored scores, and no false independence. Original Shipshape metadata concern remains current. Historical Angelcore evidence is not current execution proof; no dependency installation or suggested test command was run by this auditor.

## Received workflow audit — workflow-auditor

All eight original workflow cores and local support files were fully read: Accelerate, Adaptive, Brainstorm, Iterative, Planner, Precision, Elon, Retrofit. No commands or evaluations ran.

**Merge cautions:** Precision→Planner is a strong fit if all nine prompt sections survive and prompt-only requests do not auto-execute. Brainstorm→Planner needs real capture/convergence, stable idea lineage, preserved unusual ideas, separate novelty/feasibility, and resurrection conditions—not a generic checklist. Adaptive remains coordination; Retrofit remains compatibility preservation. Accelerate was already lean: preserve rapid complete implementation, existing-stack use, early integration tests, scoped authorization, evidence-led retries, browser checks when available, and equal-workload performance comparisons. Speed of implementation is not runtime optimization. Iterative→Shipshape must retain non-polish artifact refinement without full shipping ceremony. Elon→Shipshape must preserve ordered reduction, protected rare/emergency controls, and explicit deletion authority. This auditor did not inspect original Shipshape/Universal and therefore did not endorse their merger independently.

**Defects to avoid carrying forward:** Planner's “run or specify” completion rule, fake perfect-score loops, automatic `.spec` sprawl; Precision's zero-ambiguity questionnaire, forced confirmation, and chain-of-thought demand; Adaptive's arbitrary staffing/depth caps, blanket messaging ban, and invented capability assumptions; Brainstorm's mandatory artifact/challenge/output quotas; Elon's authoritative-source claim and blind deletion of unused controls; Iterative's “deterministic proof” overclaim and budget exhaustion as completion.

Original `iterative-refinement/evals/evals.json:85–88` falsely required declaring a Friday/Monday/Tuesday schedule infeasible. Facts support conditional risk analysis, not automatic impossibility. Do not migrate that faulty gold answer. Original catalog omitted Accelerate. Acceptance cases should test capture-in-progress, prompt-only delivery, unrun checks, injection, rare controls, migration recovery, no delegation, overlapping writers, and truthful partial results.

## Workflow-builder implementation and checks already completed

Earlier authorized changes: rewrote `planner-omega/SKILL.md` (794-word body), added three optional plan/brainstorm/prompt references and two eval fixtures; removed `brainstorm-funnel/` and `precision-architect/` after transfer. Rewrote `adaptive-orchestrator/SKILL.md` (973-word body) and aligned AGENTS, CLAUDE, INSTALL, metadata, and both eval files. No unrelated edits or commits.

Passed static checks: two JSON fixtures, two YAML frontmatters plus agent YAML, fixture types, three local links, word bounds, nine prompt headings, removed-directory absence, stale-name/cap/perfect-score/completion-rule scans, and scoped `git diff --check`. Fixtures are not live-model evaluations. Inherited DOI pointers are labeled unverified background. Global catalog/migration routing remains parent-owned.

## Final independent disposition — 2026-09-24 21:37 UTC

**PASS_WITH_RISK for the current working tree**, based on HEAD `60c44ff5831926a790bcb644ea3de0ea192e9be9` plus uncommitted repairs. This disposition supersedes the historical open findings above for the inspected scope. No skills were edited during this recheck.

**Repairs confirmed by current reads:** Shipshape metadata is mode-neutral and non-readiness work omits shipping verdicts. Explain requires escaped untrusted text, diagram text equivalents, and reduced-motion preferences. Retrofit limits review formatters and isolates authorized migration tests. Calibrate inspects the destination and requires approval of revised text. Sexyness routes accessibility, images, and non-code layers; explicit image requests override optional-generation skips; shape-only cleanup goes to code/architecture. Smoothness retains interruption, timing, FLIP, bounded staggers, cleanup, reduced motion, races, drafts, focus, idempotency, and rollback. Current source/path scans, including dotfiles, found no removed-toolkit residue; its directory is absent. Native QA safeguards remain.

Angelcore's manifest, missing-input handling, optional private reference, sandbox/loopback/offline rules, focus restoration, and source/crop/coverage records are repaired. Reviewed code SHA-256 prefixes: `audit.py` `1b1a6cf9`; `browser_check.py` `c14ca406`; `demo.js` `a9e2a0af`; `ascii_dither.py` `e3841112`; validator `461f4868`.

**Checks independently executed after reading commands:**

- `python -m unittest discover -s tests -v`: **16 passed**.
- `python -m pytest skills/angelcore-design/tests -q -rs -p no:cacheprovider`: **55 passed; 1 POSIX PTY test skipped on Windows**.
- `python scripts/validate_skills.py`: **16 skills, 8 roots, 10 JSON files; zero errors**.

Bytecode writes were disabled. Parent browser reports at `C:/Users/fate/AppData/Local/Temp/skill-browser-check-l6ro7awz/{http,offline}/browser-report.json` were inspected: **126/126 passed each**, Chrome 153.0.8010.53, sandbox enabled. I did **not** rerun the browser.

**Residual limits:** Temp-fixture probes found nonblocking robustness gaps: validator throws `ValueError` for malformed bracketed URLs and truncates valid parenthesized Markdown paths; Angelcore audit accepts empty contrast lists with zero contrast checks. Current inputs do not use these cases. Browser checks still lack explicit HTTP image-decode assertions and do not establish cross-browser, physical-device, screen-reader, or full accessibility conformance. Behavioral fixture documents are not model-behavior test passes. No installs, commits, network, or new agents.

**21:40 UTC closeout:** Parent added standalone YAML validation during review. Re-read validator `b1e12726` and its new regression: **17 unit tests passed**; final validator also checked **2 YAML files**, with zero errors. The edge-case limits above remain.
